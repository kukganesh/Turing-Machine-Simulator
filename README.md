# Turing-Machine-Simulator
A Turing Machine simulator that demonstrates the Chomsky Hierarchy by showing how a TM can simulate both DFA (for regular languages via regex) and PDA (for context-free patterns like aᵏⁿbˡⁿ) with interactive step-by-step visualization of state transitions, tape operations, and stack behavior.
